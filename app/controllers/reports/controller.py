"""Reports HTTP routes.

Cycle export stub is mounted on the cycles controller at
`GET /api/v1/cycles/{cycle_id}/export`. This package is reserved for
future report endpoints.
"""

from fastapi import APIRouter

router = APIRouter()
